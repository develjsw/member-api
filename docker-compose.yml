version: '3.9'

services:
  mysql-db:
    image: mysql:latest
    container_name: mysql-server1
    ports:
      - "33061:3306"
    #############################################################
    # - env 정보 관리 방법 변경 필요 -
    # 1. env_file로 관리하는 방법
    # 2. docker swarm을 사용한다고 가정할 때 docker secret을 통해 관리
    # 3. kubernetes의 경우는 어떻게 관리하는지 확인 필요
    #############################################################
    environment:
      - MYSQL_ROOT_PASSWORD=test1234//
      - MYSQL_USER=admin
      - MYSQL_PASSWORD=test1234//
      - MYSQL_DATABASE=msa
    restart: always
    networks:
      - msa-api-network

  member-api:
    image: member-api:latest
    build:
      context: .
      dockerfile: ./dockerfile/Dockerfile-local
    ports:
      - "3001:8001"
    environment:
      - PORT=8001
      - NODE_ENV=local
      - MYSQL_HOST_ENV=mysql-db
    restart: always
    networks:
      - msa-api-network
    depends_on:
      - mysql-db

networks:
  msa-api-network: